// Enum representing different route paths
export enum RoutePath {
  HOME = "/",
  LOGIN = "/login",
  SIGN_UP = "/sign-up",
  DASHBOARD = "/dashboard",
}

// Enum representing different route titles
export enum RouteTitle {
  HOME = "Home",
  LOGIN = "Login",
  SIGN_UP = "Sign Up",
  DASHBOARD = "Dashboard",
}
